/** /uitest/onboarding — création de compte en moins de 30 secondes. */

import { OnboardingFlow } from '@/components/alfy/auth/onboarding-flow';

export default function UitestOnboardingPage() {
  return <OnboardingFlow />;
}
